import { StatisticsView } from "@/features/statistics/components/statistics-view";

export const metadata = { title: "إحصائيات المراقبين" };

export default function StatisticsPage() {
  return <StatisticsView />;
}
